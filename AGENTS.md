## Agent skills

### Issue tracker

Issues are tracked on GitHub Issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the canonical five labels with no overrides. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context — `CONTEXT-MAP.md` at root points to per-module `CONTEXT.md` files. See `docs/agents/domain.md`.

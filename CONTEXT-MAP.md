# Context Map

This is a multi-context repo. Each context lives in its own `CONTEXT.md` file, referenced below.

| Context | Description | CONTEXT.md |
|---------|-------------|------------|
| Core business | Alutec's domain model — customers, suppliers, products, sales, installations | `CONTEXT.md` |

(Add new rows here as additional contexts are identified, e.g. for a separate web app module, API, or mobile client.)
